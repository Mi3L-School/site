import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "2026-2027 V5RC Competition Schedule",
  description:
    "See the full 2026-2027 competition calendar for teams 2055A, 2055X MS, and 2055C MS, from local qualifiers to the World Championship.",
};

export default function ActivitiesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
