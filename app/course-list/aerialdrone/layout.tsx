import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "RECF Aerial Drone Program & Team Training",
  description:
    "Join MI3L School's RECF Aerial Drone Program for grades 5-12 — weekly team training in precision flight, autonomous Python coding, and competition strategy.",
};

export default function AerialDroneCourseLayout({ children }: { children: React.ReactNode }) {
  return children;
}
